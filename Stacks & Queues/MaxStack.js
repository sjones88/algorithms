// We want to be able to access the largest element in a stack
// using the following stack implementation

function Stack() {
	this.items = [];
}

Stack.prototype.push = function (item) {
	// remember: a stack is first-in, last-out data structure...
	
}